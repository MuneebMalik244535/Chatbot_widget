# This is a public project, no sensitive environment variables needed
# The chatbot runs entirely on the frontend with mock data

# Optional: for development
NEXT_PUBLIC_API_URL=http://localhost:3000
